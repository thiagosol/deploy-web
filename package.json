{
  "name": "deploy-web",
  "version": "1.0.0",
  "description": "Painel de Deploy",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && lite-server --baseDir public"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.3.2",
    "lite-server": "^2.6.1"
  }
}
